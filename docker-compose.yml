services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: markdown_reviewer
      POSTGRES_PASSWORD: markdown_reviewer
      POSTGRES_DB: markdown_reviewer
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
